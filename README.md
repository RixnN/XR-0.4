# XR-0.4
1. Unity платформа WebGL
Edit > Project Settings > Package Manager > Scoped Registries
Windows > Package Manager > установить WebXR Export и WebXR Interaction
Windows > WebXR
Edit > Project Settings > Player > Resolution and Presentation > WebGL Templet > WebXR2020
Windiw > Package Manager > 

2. Git
ssh-keygen //много раз enter
cat C:\Users\student\.ssh\id_ed25519.pub // скопировать что вывело
Github > Settings > SSH and GPG keys > new SSH key > вставить в key
git clone (Вставить с Github > repositoriu > code > SSH)
cd .\(название репозитория)\
В проводнике закинуть билд в папку репозитория
git add *
git commit -m "c.1"
git push
